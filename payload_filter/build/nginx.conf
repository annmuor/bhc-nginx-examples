error_log /dev/stderr debug;
load_module /etc/nginx/libpayload_filter.so;
daemon off;
events {}
http {
    server {
        listen 8080;
        location / {
            proxy_pass http://127.0.0.1:8081/;
            proxy_request_buffering on;
        }
    }
}